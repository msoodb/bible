#!/bin/bash

ls, cat, cd, pwd, echo
touch, cp, mkdir, mv, rm, file, wc, file
find
grep
& && > >>
nano, vim, emacs
su
crontab
curl, wget
ssh, scp
man
ps, top, htop, kill, pkill, fg, &
systemctl
apt, yum, dnf
firewall
selinux
/etc, /var, /root, /tmp, /home , /etc/passwd
log
useradd, passwd, usermod
